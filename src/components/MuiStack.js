import React from 'react';
import {Stack} from '@mui/material'

function MuiStack(props) {
    return (
        <Stack sx={{border: '1px solid'}} direction>

        </Stack>
    );
}

export default MuiStack;
